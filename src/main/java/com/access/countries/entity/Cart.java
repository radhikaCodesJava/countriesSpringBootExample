package com.access.countries.entity;

public class Cart {
     //every user has one cart so one to one, it can be empty , max size of cart is 30 products 
	//list of products in cart- one cart can have many products so @onetoMany
	//list of products in saveforlater
	//list of products in wish list
	
	//txn details
	//every cart should have checkout option - boolean field if true then take to billing page
	// payment screen,
}
